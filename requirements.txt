# Nova v207 — Python LiveKit Agents + Runway plugin + Postgres memory
# Loora-grade brain architecture (Jun 5 2026)

# Core LiveKit Agents framework + plugins
livekit-agents[runway,deepgram,elevenlabs,openai,silero,google]>=1.5.0,<2.0.0

# Gemini Flash for vision ("she sees me" moment, separate from brain)
google-genai>=1.0.0

# Postgres for persistent kid memory across deploys (optional — falls back to RAM if no DATABASE_URL)
psycopg2-binary>=2.9.9

# FastAPI HTTP server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Env loading
python-dotenv>=1.0.0

# HTTP client (used by vision module)
httpx>=0.27.0
